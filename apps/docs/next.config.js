/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    domains: ["tse4.mm.bing.net", "tse1.mm.bing.net", "tse2.mm.bing.net"] , // Add all necessary domains
  },
};

export default nextConfig;
